server:
  port: 8080

spring:
  application:
    name: Book
  datasource:
    username: erinbro
    url: jdbc:postgresql://librimem_db:5432/book
    password: erinbro
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

