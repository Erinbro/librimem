info:
  app:
    name: Citation
    description: Manage the citations of a section
    version: 1.0.0
  company:
    name: Erinbro

server:
  port: 8087

spring:
  application:
    name: citation
  datasource:
    username: erinbro
    url: jdbc:postgresql://postgres:5432/librimem
    password: erinbro
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true