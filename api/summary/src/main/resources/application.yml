info:
  app:
    name: Summary
    description: Mange the summary of the chapters
    version: 1.0.0
  company:
    name: Erinbro

server:
  port: 8084
  error:
    include-message: always
    include-binding-error: always
    include-stacktrace: always

spring:
  application:
    name: summary
  datasource:
    username: erinbro
    url: jdbc:postgresql://localhost:5432/summary
    password: erinbro
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true