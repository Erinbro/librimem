/**
 * Service that handles the interaction
 * between IndexedDB and the backend state
 */
export class StorageService {

  /**
   * Gets the difference between the storage
   * in IndexedDB and the backend id est what
   * is in the redux store
   */
  getDifference() {
  }

  sendDifferenceToBackend() { }

}
